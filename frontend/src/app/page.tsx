/**
 * Home page (scaffold stub).
 * The real dashboard, layout shell, and routes are implemented in later tasks
 * (see tasks 12.x and 13.x).
 */
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center p-24">
      <h1 className="font-display text-6xl text-white">Inventory Management Dashboard</h1>
    </main>
  );
}
