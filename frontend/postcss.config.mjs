/**
 * PostCSS config for Tailwind CSS 4.
 * Tailwind 4 ships its PostCSS integration via @tailwindcss/postcss.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
